

def generate_uniform_regular_family(n, k):
    """
    Attempts to randomly generate a family of sets F s.t.
    |F| = n
     F is k-uniform, i.e. all sets in F are of size k
     F is k-regular, i.e. all x in [1, ..., n] belong to k sets in F
    """
    to_return = []
    pass


if __name__ == '__main__':
    A = generate_uniform_regular_family(n, k)
    print(A)
